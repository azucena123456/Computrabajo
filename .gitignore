# Dependencias de Node.js (nivel raíz y subdirectorios)
node_modules/
backend/node_modules/
frontend/node_modules/

# Archivos relacionados con Expo
backend/.expo/ # Expo está dentro de backend según tu imagen

# Configuraciones de VSCode
.vscode/            # .vscode en la raíz
backend/.vscode/    # .vscode dentro de backend
frontend/.vscode/   # .vscode dentro de frontend

# Carpetas de datos en el backend
backend/data/

# Archivos de registro
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Variables de entorno
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Salidas de compilación (para el frontend)
# Estas carpetas suelen contener la versión compilada de tu aplicación frontend
# y generalmente no deben subirse al repositorio.
frontend/build/
frontend/dist/

# Archivos del sistema operativo
.DS_Store
Thumbs.db

# Archivos ejecutables para WhatsApp (asumiendo que no son parte del código principal que necesita ser versionado)
ejecutar_whatsapp.bat
enviar_whatsapp.py

# Archivos de base de datos local o caches específicos de tu proyecto
# Si 'PyWhatKit_DB.txt' es una base de datos generada localmente y no parte del código fuente
frontend/PyWhatKit_DB.txt